language: java
sudo: required

services:
  - docker
  
after_success:
  - docker login -u joelcrisanto -p $DOCKER_PASSWORD
  - docker build --tag dspringboot .
  - docker tag firstimage joelcrisanto/dspringboot
  - docker push joelcrisanto/dspringboot
